#include <stdio.h>
/**
 * main - Entry point of program
 *
 * Description: This program prints the alphabet in lowercase,
 * follwed by a new line, using only putchar funtion.
 *
 * Return: Always 0 (succcess)
 **/
int main (void)
{
	char letter;
	for (letter 'a', letter <= 'z',letter ++)
		putchar(letter);
	putchar('\n');

	return(0);

}
